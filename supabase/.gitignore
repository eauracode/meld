# Supabase
.branches
.temp

# Generated by `pnpm build:functions` (scripts/bundle-edge-functions.mjs) —
# bundles packages/ledger + packages/payments for Deno; rebuild, don't commit.
functions/_shared/

# dotenvx
.env.keys
.env.local
.env.*.local
