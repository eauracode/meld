/** CSV template contract for bulk order import (shared by the action and the UI). */
export const CSV_HEADER =
  "customer_name,customer_phone,delivery_address,delivery_state,delivery_area,payment_type,sku,quantity,order_value_naira";
